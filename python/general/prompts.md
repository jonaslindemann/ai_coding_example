Create an example qt application deriving from QMainWindow with a simple menu and event methods for all menu items.

Create a python script that traverses a directory tree. It should have command line options for specifying the start of the search otherwise use the current working directory.